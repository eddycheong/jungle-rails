<% if product = Product.find_by(id: line_item.product_id) %>
  <tr>
    <td> 
      <h4><%= product.name %></h4>
      <br>
      <p><%= product.description %></p>
    </td>
    <td>
      <%= line_item.quantity %>
    </td>
    <td>
      <%= Money.new(line_item.total_price_cents)  %>
    </td>
  </tr>
<% end %>